#include <stdio.h>
/**
 * main- start point of the program
 *
 * Return: returns a default value of 0
 */

int main(void)

{
	int i;

	for (i = 122; i >= 97; i--)
		putchar(i);

	putchar(10);
	return (0);
}
