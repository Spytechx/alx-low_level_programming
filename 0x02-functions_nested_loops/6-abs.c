#include "main.h"

/**
 * _abs - Start point
 * @x: takes input
 * Description: FUnction returns 0 or 1
 * Return: 0 is false and 1 is true
 */

int _abs(int x)
{
	if (n < 0)
	{
		return (x * -1);
	}
	return (n);
}
